//
// Copyright 2018 eBay Corporation
// Author: Brian Szmyd <bszmyd@ebay.com>

syntax = "proto3";

import "raft_types.proto";

package sds;

/// This is a VERY basic service. A service might include this RPC as
/// one of the calls in a greater service.
service RaftSvc {
  rpc Step (RaftMessage)  returns (RaftMessage);
}
