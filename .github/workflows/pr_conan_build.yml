name: Nuraft Mesg PR Build

on:
  pull_request:
    branches:
      - main

jobs:
  BuildCommit:
    strategy:
      fail-fast: true
      matrix:
        build-type: ["Debug", "Release"]
        malloc-impl: ["libc"]
        prerelease: ["False"]
    uses: ./.github/workflows/build_commit.yml
    with:
      build-type: ${{ matrix.build-type }}
      malloc-impl: ${{ matrix.malloc-impl }}
      prerelease: ${{ matrix.prerelease }}
